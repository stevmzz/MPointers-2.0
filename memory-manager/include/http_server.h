#ifndef HTTP_SERVER_H
#define HTTP_SERVER_H

#include <memory>
#include <pistache/endpoint.h>
#include <pistache/router.h>
#include "memory_manager.h"

/**
 * Clase que implementa el servidor HTTP utilizando Pistache.
 */
class HttpServer {
public:
    // Aquí irían:
    // Constructor que inicializa el servidor
    // setupRoutes()
    // start()
    // stop()
    // handleCreate(const Pistache::Rest::Request&, Pistache::Http::ResponseWriter)
};

#endif // HTTP_SERVER_H