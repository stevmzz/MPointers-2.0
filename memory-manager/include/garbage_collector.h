#ifndef GARBAGE_COLLECTOR_H
#define GARBAGE_COLLECTOR_H

#include <memory>
#include "memory_manager.h"

/**
 * Clase que implementa el recolector de basura.
 */
class GarbageCollector {
public:
    // Aquí irían:
    // Constructor que inicializa el garbage collector
    // start()
    // stop()
    // collectGarbage()
    // setInterval(int milliseconds)
    // isRunning()
};

#endif // GARBAGE_COLLECTOR_H