#ifndef HTTP_CLIENT_H
#define HTTP_CLIENT_H

#include <string>

/**
 * Clase que implementa el cliente HTTP para comunicarse con el Memory Manager.
 */
class HttpClient {
public:
    // Aquí irían:
    // Constructor que inicializa el cliente
    // create(size_t size, const std::string& type)
    // set(int id, const std::string& value)
    // get(int id)
    // increaseRefCount(int id)
    // decreaseRefCount(int id)
};

#endif // HTTP_CLIENT_H