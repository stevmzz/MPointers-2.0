#ifndef MPOINTER_H
#define MPOINTER_H

#include <string>

/**
 * Clase template que implementa un puntero inteligente remoto.
 */
template <class T>
class MPointer {
public:
    // Aquí irían:
    // Constructor y destructor
    // Método estático Init(int port)
    // Método estático New()
    // Sobrecarga del operador *
    // Sobrecarga del operador &

    // También se necesitan:
    // Sobrecarga del operador =
    // Métodos para comunicarse con el servidor
};

#endif // MPOINTER_H